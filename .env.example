# =============================================================================
# Shop Chat Agent Environment Configuration
# =============================================================================

# =============================================================================
# Network & Server Configuration
# =============================================================================
# Host IP to bind the container to (0.0.0.0 for all interfaces)
HOST_IP=0.0.0.0

# Port for the main application (default: 3000)
PORT=3000

# Frontend development port (default: 8002)
FRONTEND_PORT=8002

# Hot Module Replacement server port (default: 8002)
HMR_SERVER_PORT=8002

# Host for the application
HOST=0.0.0.0

# =============================================================================
# Node.js Environment
# =============================================================================
# Environment: development, production, test
NODE_ENV=production

# =============================================================================
# Shopify App Configuration
# =============================================================================
# Your Shopify App API Key (required)
SHOPIFY_API_KEY=your_shopify_api_key_here

# Your Shopify App API Secret (required)
SHOPIFY_API_SECRET=your_shopify_api_secret_here

# Your Shopify App URL (required)
SHOPIFY_APP_URL=https://your-app-domain.com

# OAuth redirect URL (required)
REDIRECT_URL=https://your-app-domain.com/auth/callback

# App scopes (comma-separated)
SCOPES=read_products,write_products,read_customers,write_customers

# Custom shop domain (optional)
SHOP_CUSTOM_DOMAIN=

# =============================================================================
# Claude AI Configuration
# =============================================================================
# Claude API Key for AI chat functionality (required)
CLAUDE_API_KEY=your_claude_api_key_here

# =============================================================================
# Database Configuration
# =============================================================================
# Database URL (default: SQLite file)
DATABASE_URL=file:dev.sqlite

# =============================================================================
# Optional: PostgreSQL Database (if you want to use PostgreSQL instead of SQLite)
# =============================================================================
# DATABASE_URL=postgresql://username:password@localhost:5432/shop_chat_agent

# =============================================================================
# Optional: MySQL Database (if you want to use MySQL instead of SQLite)
# =============================================================================
# DATABASE_URL=mysql://username:password@localhost:3306/shop_chat_agent
